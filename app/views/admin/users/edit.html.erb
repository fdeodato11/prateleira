<div class="admin-header">
  <h1>Edit User: <%= @user.email_address %></h1>
  <div class="admin-actions">
    <%= link_to admin_users_path, class: "btn" do %><%= icon("arrow-left") %> Back<% end %>
  </div>
</div>

<div class="form-card">
  <%= form_with model: [:admin, @user] do |form| %>
    <div class="form-group">
      <label class="checkbox-label">
        <%= form.check_box :admin %>
        Admin
      </label>
    </div>

    <div class="form-actions">
      <%= form.button class: "btn btn-primary" do %><%= icon("check") %> Update<% end %>
      <%= link_to admin_users_path, class: "btn" do %><%= icon("x") %> Cancel<% end %>
    </div>
  <% end %>
</div>
