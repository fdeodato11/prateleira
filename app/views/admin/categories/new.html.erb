<div class="admin-header">
  <h1><%= t("labels.new_category") %></h1>
  <div class="admin-actions">
    <%= link_to admin_categories_path, class: "btn" do %><%= icon("arrow-left") %> Back<% end %>
  </div>
</div>

<%= render "form", category: @category %>
